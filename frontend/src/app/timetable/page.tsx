import React from 'react';
import { PlaceholderModule } from '@/components/ui/PlaceholderModule';

export default function TimetablePage() {
  return (
    <PlaceholderModule
      moduleName="Academic Timetable"
      description="Automated timetable generation, classroom conflict resolution, and lab allocation schedules."
    />
  );
}

import React from 'react';
import { PlaceholderModule } from '@/components/ui/PlaceholderModule';

export default function AiAssistantPage() {
  return (
    <PlaceholderModule
      moduleName="AI Academic Assistant"
      description="Natural language query interface, automated schedule optimization assistant, and intelligent workload recommendation agent."
    />
  );
}
